> **Superseded:** see `Aug31_State.md` for the current state, reconstructed and verified against HEAD `d31b10c` (2026-08-31).

Verified facts for the plan:
1. Struct chain: TriGpu (C++, include/mesh.h, sizeof 100) is the single struct used on both sides:
- CPU: MeshObj.tris (scene.h:31), MeshObjData.tris (types.h:39), EmissiveSurf.tris (types.h:80) are all TriGpu*.
- CPU consumers: bvh.cc tri_bbox/centroid (reads v), hit_mesh_bvh in renderer.cc (v), tri_area, sample_emissive_mesh (v,n); g_hit_mesh_linear in denoiser.
- GPU: raw bytes are copied via struct assignment into gpu_renderer.mm (all_tris copy loop 299-305, which then overwrites mesh_idx — note: all_tris[off] = scene->meshes[m].tris[t] copies the entire struct, including mesh_idx, and then overwrites it with m); bvh_build reorders inside a new array via ordered[out_idx] = tris[orig] + a final memcpy (bvh.cc:253-286).
- MSL mirror: shaders.metal:97-102, packed_float3/2 — align-1 throughout, exactly 100 bytes, strict byte-for-byte match with C++. Adding three packed_float4s (packed_float4 has size 16 align 1) keeps it at exactly 148 bytes — no implicit padding.
Wait — let me check MSL struct alignment: a struct with all packed types has alignment 1 and size 148. C++ struct: members are float4 (align 4) → alignof 4, size 148 (no trailing padding since 148 is a multiple of 4… 148 = 4×37, yes). Identical memory layout. However, in MSL, reading packed_float4 members requires care (unaligned access is fine, but reads emit four scalar loads? No — does MSL support vectorization of packed member loads? Reading packed members isn't directly auto-vectorizable; in practice you construct float3/4 from the components. In step 1 there is no kernel-side read so it's fine. In step 2, shader code will read tangents via explicit element indexing.)
2. Producers (TriGpu writers) — the claim of "54 sites" is outdated and it's actually 3:
- obj_parser.cc:73-173 load_obj: fan-triangulation → soup; fills v/n/t field-by-field from shared OBJ vertex/texcoord/normal tables; the face parser's parse_idx records per-corner v/vt/vn and then, during the expansion loop (134-169), flattens them into triangle soup. The shared vertex tables (verts/texcs/norms) do exist at that point — this is the OBJ pre-bake hook point. World transform is applied later at parser.cc:321-323 (uniform scale + translation only).
- gltf_parser.cc:1735-1805 (inside the "third pass: walk node tree, bake transforms, fill output" starting at :1967… wait, that says line 1967 — but the bake block I read is 1728-1806 which is before the 1967 function? So this bake is inside a different earlier function. Anyway — comment :1504 says the node tree walk bakes transforms; world matrices are computed per-node; m4_transform/m4_transform_normal is used inside the bake loop.)
- glTF primitives are indexed: pd->indices + pd->num_indices (decoded from accessors, :575, :731); non-indexed primitives are skipped entirely (if (!pd->positions || !pd->normals || !pd->indices) continue; :1751 — primitives without indices get zero triangles, don't render). Indexed, shared vertices → MikkTSpace can be run directly against pd->positions/normals/texcoords/indices with per-vertex callbacks + a tangent output array indexed by vertex index. Exactly as phase3_plan was guessing. No welding step needed for glTF.
- Transform application is inside the per-primitive bake loop (1766-1771), after per-triangle soup extraction. So glTF tangent flow: (a) before bake, run MikkTSpace on primitive's shared vertices → per-vertex tangent array; (b) inside the existing bake loop, copy t→3 per triangle vertex, rotate with m4_transform (not m4_transform_normal — tangent is a direction vector not a normal; under non-uniform scale the two differ → use m4_transform like position, then re-orthogonalize against the transformed normal, Gram-Schmidt), then store into TriGpu with handedness in w.
- Wait — phase3_plan said "transform tangents into world space together with the normal." Using m4_transform (full linear part) and then Gram-Schmidt against the transformed normal, while the normal uses m4_transform_normal (probably inverse-transpose or rotation-only?), is correct standard practice. Yes: tangent should be transformed by the full linear part (like position/tangent direction), then orthogonalized against the transformed normal.
- producer #3: gpu_renderer.mm's combined-array copy (299-305) — pure struct assignment, propagates automatically.
3. The "54 sites" in phase3_plan is wrong/outdated — there is no brace-initialization TriGpu site; everything is allocation (calloc/malloc) + field-by-field fill or whole-struct copy. Step-1 edits touch about 6 files, not 54 sites. This is a correction worth flagging in the plan (phase3_plan says "report if anything has moved" — this has moved. The plan's struct chain MeshObj→MeshObjData→TriGpu is real, but "54 sites" is probably a site count under an older tri format, or just wrong.)
4. TriGpu consumers that must keep working without change (all read-only for v/n/t): bvh.cc (2 places), renderer.cc (hit_mesh_bvh, emissive mesh sampling, tri_area), shaders.metal (in_shadow ×2, sample_emissive_mesh_gpu, trace_ray's nearest + shadow tri loop), denoiser (g_hit_mesh_linear), gpu_renderer (gpu_tri_area, combined copy, buffer size via sizeof at :460), gltf_debug (--tri-debug printing).
5. Gates:
- make test's six scenes = torus, ico, vase, demo, emissive_demo, complex (Makefile:91-105). Plus scenes/scene.json (make run). Plus quick/full versions of alligator/armadillo/bimba/cheburashka/bunny etc. under scenes/ — mostly OBJ + some gltf? (scene_alligator etc. — GLTF models, probably GLTF with textures; "scene_complex.json" = "65K tri torus" OBJ).
- tools/parity.sh: only PASSes if all CPU-vs-GPU diffs are exactly 0 differing pixels and backend lines are correct; default pair = scene_iri_dish_parity256.json + scene_envtest_stdout.json.
- Test assets: test_scenes/scene_iri_dish_parity256.json (256×234) — dish, 4 materials 11 textures, no TANGENT attributes (phase3_plan), ORM-based occlusion.
- commit-delta gate = byte-identical on all six make-test scenes + scene.json + both of the parity pair's scenes, both backends, against a HEAD-baseline worktree render (AGENTS.md harness).
- Baseline recording rule: does the parity pair scene set run against HEAD worktree also have to be byte-identical under CPU-vs-GPU? No — new-vs-old is identical to the recorded cross-backend signature. Currently parity.sh requires CPU-vs-GPU of new code to be zero-diff, so… careful: recorded dish256 cross-backend signature was historically 137px, but parity.sh's diff_pair demands zero-diff… contradiction? Does "differing=0" pass but non-zero fail → parity.sh would FAIL on dish256 right now if cross-backend diff is 137px?? Unless the gap is now zero (README claims dish is now byte-identical). Hmm: AGENTS.md records 137px as "pre-change signature against the ao_tex_index plumbing commit (verified byte-identical after change on both backends)" — ambiguous, but parity.sh default scene + zero-diff requirement implies: currently, CPU-vs-GPU on those two scenes is presumed byte-identical (after the two GPU fixes landed). So step-1 gate = all pairs byte-identical + all commit-delta pairs byte-identical. Clearer than what I drafted earlier. Before launching, run parity.sh to confirm harness passes at HEAD (prerequisite check; also closes the "GPU not runnable" issue from ace5b97).
6. MikkTSpace vendoring: need to confirm source + API. Dependency candidate references to note down (canonical sourcehut/gitlab + verified mirror in GitHub tree like Assimp/godot thirdparty — the implementer picks one, no later fetch attempts… should I fetch attempts myself right now? Consumed the last batch round without fetching; one more cheap bash (curl -sI / git ls-remote) to test existence and lock the URL + pin sha256 of download. A short bash (a few seconds, write to /tmp) is worth it for plan accuracy. Then read the header (Read tool on the /tmp files) to lock the interface. That is "research", not implementation. OK, doing it — one bash: try git sparse-clone-style raw downloads (raw.githubusercontent) of mikkcspace.c/h candidates to /tmp/mikktspace_check/, sha256? and if successful show first 40 lines of header (sed -n… no — then Read). URLs to try:
- https://raw.githubusercontent.com/godotengine/godot/master/thirdparty/misc/mikkcspace/mikkcspace.h and .c (guess)
- godot newer layout: thirdparty/misc/mikkcspace/?
- canonical sourcehut too? Don't remember exactly; skip, mirror is fine, but plan should say "vendor from Khronos glTF-Sample-Models' 2.0/Shared/mikkcspace.c" hmm?
Now that I've actually remembered: KhronosGroup/glTF-Sample-Models' repo has a top-level 2.0/Shared/ containing mikkcspace.c/mikkcspace.h? Because it has KHR_texture_transform… no. There's also 作者's reference at http://www.ge….
OK — during bash execution, use webfetch via an html.duckduckgo.com html search link for "mikkcspace reference github". But that's not available here — I'll decide to do a simple curl attempt (curl is allowed; not banned here — parallel tool turns are for things not knowing their result; here we can accept some guessing on branch names: gitee engine master, 4.5?)… hmm, cheaper:
  curl -sf https://api.github.com/repos/zen生素…/contents/... no.
OK, simplest reliable method that I'm confident about: KhronosGroup/glTF-Sample-Models' default branch is master (archived). Under root is there a shared? Try it in curl (a read-only GET request to GitHub raw is cheap; combined into one bash call; default doesn't even need -fsSL and code). Then Read whichever downloaded fine (bash test will tell us if non-404/empty).
Note: unconditionally "do not introduce license-scan/compliance risk": mikkcspace.c's public-domain/MIT-style license (Blender relicense note "This library is free to use in any commercial and non-commercial projects"). Confirm header comments after fetching; cite license + source URL inside the vendored file's header block (vendor pattern = mirror directory with LICENSE/NOTICE files → follow: put the original two files + LICENSE header-checksum file under src/parser/mikkcspace/? — existing tree has no third-party pattern (tools are first-party)… so create third_party/mikkcspace/. Two files, plus a one-liner in README saying where it came from.)
Now, another thought about whether step 1 wires tangents into OBJ's load_obj. Decision: glTF-only in step 1 (test assets are gltf; per scening, OBJ assets have no normal maps — quickly verify scenes/.json referencing models/.obj that have normalTexture… eh, grep the scene jsons for '"normal' + owner scene list in bash batch). Then step-1's only shader-side change = struct mirror padding (unrestricted). Simplest to make gate proofs clean. Leave OBJ as zero sentinel.
But wait — generating only on gltf, will scenes/*.json hold six make-test + scene.json + test_scenes/scene_lamp_stdout.json + scene_iri_dish_parity256.json? Check gltf type via file-size mapping: skip — state: "scene list (six make-test + scene.json + two parity-scene files + optional gltf cross-check); if dot-textured ones, adapter also executes in OBJ pass" — enough; can be verified during impl batch later.
Also for gating purposes, scene json list: quick check whether any scene jsons point at models/*.obj or file:"…obj" or gltf. Optional; include in pre-chef-1.
Now, the interpolation-time question for step 2 (context only, cited as reason for per-vertex packing + send): GPU nearest-hit inside trace_ray — CPU renderer.cc:164/169 hit_mesh_bvh is not shared with GPU? CPU-only is OK. GPU kernel has its own tri loops (around in_shadow; main loop for trace_ray is after ~700, unread but I confirmed the "tri loop" exists with tris[i].mesh_idx == skip_mesh). OK — not needed in step 1.
One more thing to quickly verify (correctness of pre-chef's "layout audit"): gpu_renderer.mm's buffer binding, buffer(4) byte size + newBufferWithBytes pass — already confirmed :460-461 (tri_len = total_tris * sizeof(TriGpu)). auto-OK. Denoiser's g_hit_mesh_linear — consumes TriGpu only. OK.
Sketch of -Wall -Wextra warnings: float tan0[4] etc. — no problem anywhere.
Struct-name conflict: none (no existing tan0).
Risk — uninitialized tangent in non-gltf fallback: calloc → zero. If B=zero, then t0 t1 t2… no, tangent is zero too; no problem (nothing reads it in step 1; step 2 treats degenerate as sentinel).
Risk — mesh_idx + 48B: TriGpu new padding tail… no, no padding.
Non-determinism risk: MikkTSpace weld depends on compile -O2? Same algorithm across backends (CPU produces tangents once per scene load; GPU kernel reuses the same all_tris buffer!) — key structural fact: GPU does not compute tangents separately; all_tris (per-scene shared, produced on CPU) gets uploaded → single production source → no cross-backend divergence from tangent data itself is possible. Divergence only from how the kernel uses them (step 2+). Nice property to document (explains why, after step-2, cross-backend parity gate can stay at zero-diff — not just ≤baseline — unless sampling differs).
Wait, correction: on GPU the BvhNode tri_start/tri_end range is over the global all_tris array with per-mesh offsets + tri_offset (compute tri_offset array, pass NULL/0/0 before build then pass it with flag=1… gltf_debug_global_arrays(..., NULL,0,0) before, then again with all_bvh… ok, informational only).
cli check: --tri-debug / --mesh-stats currently print TriGpu info (gltf_debug.cc:315+ — const float* v[3] outputs per-tri bbox / last-tri data). Add small optional --tri-debug line in step-1: first 5 triangles per mesh, print normalized |t| + min dot(N̂,t̂) across primitives (cheaply catches degenerate/missing tangents before step-2 visual debugging). Nice per phase3_plan ("placeholder shows up here as garbage in one scene").
Plan file layout (write to /Users/jon/Dev/ray/phase3_step1_tangents_plan.md):
- Title + status + relation to phase3_plan (replace §2/§8 step-1 details; record contradictions: no 54 sites; chained sources).
- 0. Prerequisite checks (before launching): run parity.sh at HEAD (also closes the "GPU not runnable" issue from ace5b97); HEAD-baseline worktree render (4-row harness); make test green on HEAD as commit-delta baseline. Note on TEMP-DBG leftover (optional cleanup commit before start; not a blocker — gated behind RAY_GDEBUG, zero pixels when unset).
- 1. Goal / non-goal list.
- 2. Facts (from quick check; with file:line).
- 3. Design: struct changes (exact code blocks in both languages, +48-byte proof), tangent generation pipeline (callback adapter with exact names, primitive pre-bake, tangents per-primitive of; transform rule: rotate and re-normalize + Gram-Schmidt against transformed normal; degenerate → zero), scoping to glTF-only + OBJ stays zero, reason for not wiring into shading.
- 4. Execution steps (commit-split proposal within step-1? phase3_plan says 1 commit per step, and step-1 is one commit; but sub-splits are possible: (1a) layout + adapter, unwired — actually, adapter unwired → zero pixels trivially holds… keep as one commit, verify per command.)
- 5. Verification protocol (commands + expected output: make test; cross 0% against baseline worktree for six-test-scene pairs + scene.json + two parity-pair-scene files, both backends; tools/parity.sh PASS to be run twice — before (HEAD baseline from worktree) and after (new); record; --tri-debug sanity assertions (number of primitives with zero tangent, min |dot(N,t)|); disk-size delta note 48B×tri count).
- 6. Optional flags and gates (-tri-debug output).
- 7. Failure modes and fallback (generate → layout unilaterally change → unconditional gzip; degenerate tangents; weld surface count explosion; --tri-debug count verification; run-only reference harness).
- 8. Estimated diff shape (files + ops) — "flag if something moved": 54-site claim is stale (verified as wrong) — because phase3_plan explicitly asks this, do I replace §2 storage claim with actual numbers? Wait — struct comment is "54 sites do TriGpu construction or filling". My check: fill/construct = load_obj (obj_parser.cc:134-169 one loop), gltf bake (gltf_parser.cc:1754-1802 one loop), gpu copy loop (299-305), bvh reorder (bvh.cc:253-286 whole struct). Total effectively 4 places / 3 files. Since phase3_plan said "report if anything moved" — already reported above; do I embed a correction in the plan too.
Precise API from the mikkcspace reference I've been trusting (from the two vendored copies I know: callback table with m_getNumFaces, m_getNumVerticesOfFace, m_getPosition, m_getNormal, m_getTexCoord, m_setTSpace, m_setTSpaceBasic, m_setTSpaceBasic2? and fPositive/end?…) to quote, plus placeholder for reference-sha256 reference for when the vendor-download attempt fails? Instead write generically: "at time of wiring, correct against the two vendored files; expected 7-8 callbacks (including m_getNumVerticesOfFace / m_getPosition / m_getNormal / m_getTexCoord / m_setTSpaceBasic)"? Inaccuracy and "exactness" of the plan contradict… do the second-round batch (fetch attempt + further reads) right now. Then write the file + summary.
Second-round batch (one bash + parallel reads + parallel fetch attempt):
  curl -sfL for vendoring candidates (3 URLs tried in one command) + sha256 + ls; write into /tmp/mikkcspace_check/ (placeholder project name).
The earlier batch really got truncated by the error (echo ==== broke on non-ASCII chars → === not found → after ; I continued as much as possible).
Keep gate-list wording exactly this way: parity.sh (4 pair comparisons: dish256 CPU-GPU, envtest CPU-GPU) must run clean, and the commit-delta diff (new vs HEAD worktree) must be zero on the six scenes in make test + scene.json + two parity-scene files, on both backends.
Scene list for gates: six test scenes per phase3_plan + scene.json default? phase3_plan is "0 px commit delta across all six test scenes" + §7 "in scenes without normal maps… byte-identical to pre-quick baseline". "Six test scenes" = make test's six (torus, ico, vase, demo, emissive_demo, complex). Plus canonical 4 scenes (lamp_stdout, envtest_stdout, scene.json + the quick 256×234 dish scenes under test_scenes). I'll trust that and enumerate from ls later (planned).
Small subprocess split into two commits? (1a) Bare expand to 148B everywhere, zero fill; (1b) MikkTSpace fill. phase3_plan §8 says step 1 is one commit ("update all 54 build sites, vendor + wire MikkTSpace… gate: 0 px"). But its own rationale ("placeholder shows up as garbage in one scene") benefits from finer splits… however, Step-1, being unrestricted, can't produce placeholder-driven garbage until step 2. The real risk that splitting separates: (a) layout mistake (both backends change everything, obvious), (b) degenerate/wrong tangent data (invisible now; only caught by --tri-debug count assertion or step-2 visual inspection). So I'll insist on one commit but add an instrumented --tri-debug summary (pass; B==0 for all primitives with texcoords, percentage of shader-only tangents, min dot(N,t)), making later degenerate/wrong tangent data visible even before step 2. Simpler and matches phase3_plan's "1 commit per step". Instructions as sub-stages, do all commit after checking each? At fastest total is one commit — or stage commit 1A (unilateral layout expand, no read), if gate passes then commit 1B (generation + fill)? phase3_plan is explicit: "do not merge steps 1 and 2" (that's about layout vs shading). Inside step 1, making it two commits (layout vs data) further separates layout mistakes from data mistakes — I'll insist on two commits; each has independent 0px gates. Actually this isn't worse than phase3_plan and might be better ("layout bug and shading bug look different")… proposed: Commit A: TriGpu 100→148, C++ + MSL + copy sites; verification = scene-wide byte-identical, both backends. Commit B: MikkTSpace vendor + adapter + tangent generation + --tri-debug tangent audit line; verification = same byte-identical gate + audit-count assertion (pass: zero-tangent percentage, count of dot(N̂,t̂)<0.999, count of shader-only degenerate tangents — expect B==0, deg==0). If audit assertions can't cleanly separate, fall back to single commit; instruction = two commits.
Sequencing inside each commit + file enumeration. Include an outline "layout audit script/commands": printf around ~main.cc? Skip — instead a shell one-liner python3/ctypes assert in Commit A's check step? Too fine; --mesh-stats/--tri-debug idea is enough. Lightweight alternative: add to the existing --tri-debug machinery a per-mesh "tangent audit" section that prints degenerate + zero fraction (data-later at Commit B; Commit A needs nothing outside assert). And rely on byte-identical gate as proof that "nothing reads t → t never affects pixel → any single byte of any pixel differs ⇒ layout mistake" (unconditional). So commit A gate = byte-identical proof. Clearer than what I drafted earlier.
Dispatch list (files touched, concrete):
1. include/mesh.h — TriGpu extension (place inside struct, keep trailing comment) + C++ compile-time assert option.
2. src/renderer/shaders.metal — mirror padding (unrestricted) + tri struct comment if needed.
3. src/renderer/gpu_renderer.mm — no change expected (struct assignment + sizeof); explicit check if any place assumes 100B.
4. src/renderer/bvh.cc — no change (memcpy/sizeof) — confirm doesn't manually unpack TriGpu… tri_bbox/centroid reads v only, correct — reorder uses whole-struct copy — auto-OK.
5. src/parser/mikkcspace.{c,h} (new, vendored) + third_party's LICENSE notice? .c/.c placed under src/parser/mikkcspace/ (Makefile lists source files explicitly; C files listed as .c).
6. src/parser/obj_parser.cc — B: run MikkTSpace per object → per-vertex tangents → scatter into soup + rotate/GS; then degenerate zero.
7. src/parser/gltf_parser.cc — B: MikkTSpace call per primitive pre-bake + adapter callbacks (feed directly from pd->positions/pd->normals/pd->texcoords/pd->indices; 1739/1751 already skips non-indexed primitives = already handles existence of non-indexed — no wait, "skips" it! Line 1751: if (!pd->positions || !pd->normals || !pd->indices) continue; — a primitive without indices is entirely skipped (no triangles!). "If prims have no shared verts, nothing here" ≠ "handled as soup". So no soup case in gltf path. Need to confirm. Another fact corrected.) → new primitive adapter helper function generate_prim_tangents(pd, world, tri_dest...).
8. Makefile — add new .c to SOURCES (compiled via .cc pattern rule… rule matches $(SRC_DIR)/%.cc; .c doesn't match! New pattern rule needed or rename .c/.c; simplest: add explicit rule `$(BUILD_DIR)/%.
